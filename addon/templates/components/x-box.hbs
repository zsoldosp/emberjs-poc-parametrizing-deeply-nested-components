{{#if customized }}
  {{yield
    (hash
      header=(component "x-box-header" title=title)
      body=(component "x-box-body")
    )
  }}
{{else}}
{{x-box-header title=title}}
{{#x-box-body}}
{{yield}}
{{/x-box-body}}
{{/if}}
